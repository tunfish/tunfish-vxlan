---
- hosts: "all"
  become: "yes"
  remote_user: "vagrant"

  tasks:
  - name: "Install Ubuntu Cloud Keyring package"
    apt:
      state: "present"
      update_cache: "yes"
      name: "ubuntu-cloud-keyring"

  - name: "Add Ubuntu Cloud Archive and WireGuard repositories"
    apt_repository:
      repo: "{{ item }}"
      state: "present"
      update_cache: "yes"
    with_items:
    - "deb http://ubuntu-cloud.archive.canonical.com/ubuntu xenial-updates/newton main"
    - "ppa:wireguard/wireguard"

  - name: "Install Open vSwitch"
    apt:
      state: "present"
      update_cache: "no"
      name: "{{ item }}"
    with_items:
    - "openvswitch-common"
    - "openvswitch-switch"
    - "wireguard"

  #- name: "Install configuration script"
  #  template:
  #    src: "setup.sh.j2"
  #    dest: "/home/vagrant/setup.sh"
  #    owner: "vagrant"
  #    group: "vagrant"
  #    mode: "0755"
